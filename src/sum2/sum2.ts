export const sum = (a: number[], b: number) => {
    a.forEach((value1, index) => {
        a.forEach( (va))
    })
    return null;
}

/// ALGO: algo sur des tableau (trouver les index dans un tableau pour que la somme des index face une valeur target )

/// utilisation d'axios